Analyze the following API requirements and generate a single complete OpenAPI 3.0 specification in YAML format. The specification should include:

API metadata (title, version, description)
Server information
Paths with HTTP methods
Parameters (query, path, header, etc.)
Request bodies with appropriate schemas
Responses with status codes and example payloads
Components for reusable schemas if applicable
Ensure the output is valid YAML and adheres to OpenAPI 3.0 standards.
Do **not** include any Markdown fencing like ```yaml or ``` 